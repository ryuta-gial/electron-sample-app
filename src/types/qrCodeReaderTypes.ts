export const initialState: initialStateType = {
    readCodeData: null,
    canSaveCsvFile: null,
};

export type initialStateType = {
    readCodeData: null | string;
    canSaveCsvFile:null| string;
};
